import React from 'react';

export default function FooterNote() {
  return (
    <footer className="text-center text-xs text-gray-500 mt-8">
      Built with secure local parsing — card details are never uploaded.
    </footer>
  );
}
